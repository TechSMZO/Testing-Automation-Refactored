module.exports = {
  // Disable file watching so page does not auto-refresh on file changes.
  watch: false,
  // Disable script injection used for live-reload in the browser.
  injectBody: false,
  // Avoid auto-navigation behavior on save/open.
  navigate: false
};
